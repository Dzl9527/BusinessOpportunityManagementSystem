# AI 协作与编码规范 (AI Rules)

为了保持项目文档的生命力和代码架构的可维护性，AI 助手在介入本项目的开发时，必须严格遵循以下标准操作流程（SOP）：

## 1. 文档同步更新原则 (Doc-as-Code)
**【强制】** 任何涉及以下层面的代码改动，在代码编写和测试完成后，**必须主动更新相关文档**，无需用户额外提醒：
- **业务逻辑新增/修改**：必须更新 `docs/modules/` 目录下对应的业务需求或逻辑说明。
- **技术架构/依赖库变更**：必须更新或新建 `docs/decisions/` 下的架构决策记录 (ADR)，例如引入了新的数据库、鉴权体系、中间件等。
- **发布与版本追踪**：必须在项目根目录的 `CHANGELOG.md` 中的 `[Unreleased]` 节点下追加一条改动日志，分类归入 `Added`, `Changed`, `Fixed` 或 `Deprecated`。

## 2. 任务流兜底机制 (Task Check)
在生成或执行执行任务清单 (`task.md` / `implementation_plan.md`) 时，**最后一个 Task 必须固定为**：
- `[ ] 验证代码并同步更新 CHANGELOG 及 docs/ 文档`
只有当该项被打勾后，才能向用户汇报当前阶段（Phase）已彻底完成。

## 3. 代码与配置规范
- 优先遵循现有的架构风格（例如：保持现有的分层架构、使用 Spring Profiles 做环境隔离）。
- 如果要引入可能影响本地开发体验的重型组件（如 MySQL, Redis），必须保留轻量级的本地替代方案（如 H2, 内存缓存），或通过 Docker 做好开发环境的隔离。
